#!/bin/bash

cat <<-EOF
NAME
  doil salt:restart - restarts the salt server

SYNOPSIS
  doil salt:restart

DESCRIPTION
  Sometimes the salt server does not work properly due to
  unknown circumstances. This commands restarts the server

EXAMPLE:
  doil salt:restart

OPTIONS
  -h|--help displays this help message
EOF